# Accepted Evidence Cache

Place locally reviewed CSV/JSON evidence files here when running the Gold source
pipeline. These files are intentionally ignored by Git because they can contain
large source-cache payloads or source snippets that should not be published.

The derived, reviewable outputs live under `data/gold/` and `reports/gold/`.
